#!/bin/bash

# Скрипт для сборки и запуска фронтенда PushToType

set -e

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
FRONTEND_DIR="$SCRIPT_DIR/frontend"

echo "🚀 Сборка и запуск фронтенда PushToType..."

# Переходим в директорию фронтенда
cd "$FRONTEND_DIR"

# Проверяем наличие Swift
if ! command -v swift &> /dev/null; then
    echo "❌ Swift не найден. Убедитесь, что установлен Xcode или Swift Toolchain."
    exit 1
fi

echo "🔨 Сборка приложения..."
swift build

echo "📱 Запуск приложения..."
echo "💡 Приложение появится в status bar (строка меню)"
echo "🎤 Используйте клавишу пробел для записи аудио"
echo "📋 Для остановки нажмите Ctrl+C или выберите 'Выйти' в меню"

# Запускаем приложение
.build/debug/PushToType

